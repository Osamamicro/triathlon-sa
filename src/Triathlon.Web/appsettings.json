{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=triathlon;Username=postgres;Password=postgres"
  },
  "Database": {
    "Provider": "Postgres",
    "MigrateOnStartup": false
  },
  "Site": {
    "_comment": "Staging turns on the Basic-auth gate and noindex; BehindProxy turns on X-Forwarded-* handling. Both are set per deployment — see deploy/README.md. Never commit BasicAuth values.",
    "Staging": false,
    "BehindProxy": false,
    "BasicAuth": {
      "User": "",
      "Password": ""
    }
  },
  "Media": {
    "_comment": "Root empty means wwwroot/media under the content root. On a server, point it outside the deployment folder so a redeploy cannot delete uploaded content.",
    "Root": "",
    "PublicPrefix": "/media"
  },
  "Email": {
    "_comment": "An empty Host disables real sending and logs instead. User and Password belong in environment variables, never here.",
    "Host": "",
    "Port": 587,
    "UseStartTls": true,
    "User": "",
    "Password": "",
    "FromName": "Saudi Triathlon Federation",
    "FromAddress": "no-reply@triathlon.sa"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment": "Every deployment sets the AllowedHosts environment variable to its own host names (e.g. triathlon.sa;www.triathlon.sa); '*' is for developer machines only.",
  "AllowedHosts": "*"
}

@using Microsoft.AspNetCore.Components.Endpoints
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <ResourcePreloader />
    <link rel="stylesheet" href="_content/MudBlazor/MudBlazor.min.css" />
    <link rel="stylesheet" href="@Assets["dashboard.css"]" />
    @* No <ImportMap />: it renders an inline <script type="importmap">, which the dashboard's
       Content-Security-Policy (script-src 'self') blocks, and nothing here needs it — every
       script on this page is referenced through @Assets[...] already. If a component ever
       imports a fingerprinted JS module, the map has to come back with a CSP nonce. *@
    <link rel="icon" type="image/png" href="@Assets["favicon.png"]" />
    <HeadOutlet />
</head>

<body>
    <Routes @rendermode="PageRenderMode" />
    <script src="_content/MudBlazor/MudBlazor.min.js"></script>
    <script src="@Assets["_framework/blazor.web.js"]"></script>
</body>

</html>

@code {
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    // Interactive everywhere except the pages that post a real form (sign in, change password):
    // those are marked [ExcludeFromInteractiveRouting] and render as plain server HTML.
    private IComponentRenderMode? PageRenderMode =>
        HttpContext.AcceptsInteractiveRouting() ? InteractiveServer : null;
}

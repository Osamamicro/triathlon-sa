namespace Triathlon.Web.Domain.Content;

/// <summary>Where a navigation link appears: the header bar, or one of the footer's two link columns.</summary>
public enum NavLocation
{
    Header = 1,
    FooterCompete = 2,
    FooterInvolved = 3,
}
